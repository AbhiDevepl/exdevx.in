# Server-only. NO VITE_ prefix — must never reach the client bundle.
# Local: copy to .env.local and fill in. Vercel: Project Settings → Env Variables.
GROQ_API_KEY=your_groq_api_key_here

# URL where the app is hosted (self-referential links).
APP_URL=http://localhost:5000
